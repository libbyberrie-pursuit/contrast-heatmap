# Contrast-heatmap
An experiment with jimp to run px-by-px contrast ratio calculations, in order to generate a heatmap of contrast invalidation. 

Built with node v 25.2.1 (NPM version 11.6.2) - you may need to specify node version using NVM (or equivalent) when running this locally/outside of a container. 